# Changelog

## [0.3.0]

### Added

- `MoneyFold.Fold` and `MoneyFold.TryFold`: the checked-i64 balance fold, pinned
  to `@pwngh/money`'s `fold.vectors.json`. Engine-independent runtime assembly,
  zero dependencies.
